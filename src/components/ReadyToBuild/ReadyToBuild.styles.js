import styled from "styled-components";

export const ReadyToBuildWrapper = styled.section`
  padding: 14rem 0;
`;

export const ReadyToBuildContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
`;

export const ReadyToBuildTitle = styled.h1`
  color: var(--very-dark-cyan);
  font-size: 1.8rem;
  text-align: center;
  margin: 2rem 0;
`;
